function search(nums, target) {
  // 1 先判断中间点
  const mid = Math.floor(nums.length / 2);
  if (target >= nums[0]) {
    if (nums[mid] > target) {
      //前面一半
    } else {
      // 后面一半，最大不大则break
    }
  } else {
    if (nums[mid] > target) {
      //前面一半
    } else {
      // 后面一半，最大不大则break
    }
  }
}
